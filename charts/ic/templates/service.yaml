apiVersion: v1
kind: Service
metadata:
  name: {{ include "ic.fullname" . }}
  labels:
    {{- include "ic.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ic.selectorLabels" . | nindent 4 }}
